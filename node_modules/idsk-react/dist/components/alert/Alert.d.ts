/// <reference types="react" />
export interface AlertProps {
    message: string;
    description?: string;
    type?: "error" | "success";
    visible?: boolean;
}
export default function Alert({ message, type, description, visible }: AlertProps): JSX.Element | null;
