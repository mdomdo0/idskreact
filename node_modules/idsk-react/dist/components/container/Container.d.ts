import React from "react";
export interface ContainerProps {
    children: React.ReactNode;
    className?: string;
    main?: boolean;
}
export default function Container({ children, className, main }: ContainerProps): JSX.Element;
