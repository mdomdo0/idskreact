import React from "react";
declare type GridWidthType = "row" | "full" | "half" | "third" | "two-thirds" | "quarter";
export interface GridProps {
    children: React.ReactNode;
    className?: string;
    width?: GridWidthType;
}
export default function Grid({ children, className, width }: GridProps): JSX.Element;
export {};
