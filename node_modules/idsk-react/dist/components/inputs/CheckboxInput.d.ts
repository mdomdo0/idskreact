/// <reference types="react" />
import InputProps from "./input";
interface CheckboxInputProps extends InputProps {
    small?: boolean;
    checked: boolean;
}
export default function CheckboxInput({ name, className, defaultValue, disabled, error, errorClassName, helperClassName, helperText, label, labelClassName, value, type, onChange, checked, small }: CheckboxInputProps): JSX.Element;
export {};
