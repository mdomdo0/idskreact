/// <reference types="react" />
import InputProps from "./input";
interface SelectInputProps extends InputProps {
    options: Array<{
        value: string | number;
        label: string;
    }>;
}
export default function SelectInput({ name, options, className, defaultValue, disabled, error, errorClassName, helperClassName, helperText, label, labelClassName, value, onChange }: SelectInputProps): JSX.Element;
export {};
