import React from "react";
interface TableColumnProps<DATA> {
    key: keyof DATA;
    label: string;
    render?: (values: DATA) => React.ReactNode;
    align?: "left" | "right";
}
interface TableProps<DATA> {
    columns: Array<TableColumnProps<DATA>>;
    data: DATA[];
    primaryKey: keyof DATA;
    title?: string;
    withHeader?: boolean;
}
export default function Table<DATA>({ columns, data, primaryKey, title, withHeader }: TableProps<DATA>): JSX.Element;
export {};
