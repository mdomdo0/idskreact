import React from "react";
export interface TabPanelProps {
    children: React.ReactNode;
    index: number;
    tabIndex: number;
    title?: string;
}
export default function TabPanel({ children, index, tabIndex, title }: TabPanelProps): JSX.Element;
