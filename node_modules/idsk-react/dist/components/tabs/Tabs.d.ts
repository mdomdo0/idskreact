import React from "react";
import { TabPanelProps } from "./TabPanel";
interface TabsProps {
    children: React.ReactElement<TabPanelProps> | Array<React.ReactElement<TabPanelProps>>;
    onChange: (index: number) => void;
    tabs: Array<{
        id: string;
        tab: string;
    }>;
    title?: string;
    tabIndex: number;
}
export default function Tabs({ tabIndex, children, onChange, tabs, title }: TabsProps): JSX.Element;
export {};
